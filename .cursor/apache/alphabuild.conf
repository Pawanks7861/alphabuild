<VirtualHost *:8080>
    ServerName localhost
    DocumentRoot /workspace

    <Directory /workspace>
        Options FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>

    ErrorLog ${APACHE_LOG_DIR}/alphabuild-error.log
    CustomLog ${APACHE_LOG_DIR}/alphabuild-access.log combined
</VirtualHost>
